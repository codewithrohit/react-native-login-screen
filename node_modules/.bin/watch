#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../react-native/node_modules/jest-haste-map/node_modules/watch/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../react-native/node_modules/jest-haste-map/node_modules/watch/cli.js" "$@"
  ret=$?
fi
exit $ret
